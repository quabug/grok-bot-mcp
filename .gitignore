# secrets & runtime
secrets/
logs/
run/
node_modules/
runtime/node_modules/
oauth-gateway/node_modules/
*.log
.env
.env.*

# agent-bridge mailbox (live queue; keep code/scripts only)
agent-bridge/outbox/
agent-bridge/inbox/
agent-bridge/sent/
agent-bridge/NOTIFY
agent-bridge/agents.json
agent-bridge/webhook.url
agent-bridge/notify.log

# OS / editor
.DS_Store
*.swp

# machine-local / live ops
STATUS.txt

# downloaded / local binaries (never commit)
bin/cloudflared
runtime/bin/cloudflared
**/cloudflared-linux-*
*.amd64
