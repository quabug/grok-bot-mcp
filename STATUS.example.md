# Runtime status (example)

Copy to `STATUS.txt` locally if you want an ops scratchpad. Do not commit real URLs or passwords.

- Public MCP URL: `https://<tunnel>/mcp`
- Auth: OAuth (owner password in `secrets/OWNER_PASSWORD.txt`)
- Local MCP: `http://127.0.0.1:3851/mcp`
- Gateway: `http://127.0.0.1:3860/`
- Workspace: `$GROK_BOT_WORKSPACE`
- Agent-bridge notify: `http://127.0.0.1:3861/notify`
